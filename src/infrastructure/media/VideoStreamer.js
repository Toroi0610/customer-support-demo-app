/**
 * VideoStreamer - infrastructure component for camera frame capture.
 *
 * Re-exports the existing VideoStreamer from media-utils.js so the
 * infrastructure layer has a canonical import path.
 */

export { VideoStreamer } from "../../utils/media-utils.js";
